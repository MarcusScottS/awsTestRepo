package utopiaAirlines;

import org.junit.jupiter.api.Test;

import java.sql.SQLException;
import java.util.ArrayList;
import java.util.List;
import java.util.Random;

import static org.junit.jupiter.api.Assertions.*;

import utopiaAirlines.basic.Flight;
import utopiaAirlines.basic.FlightDao;
import utopiaAirlines.basic.FlightUpdateDao;

public class FlightDaoTest {


				@Test
			    public void testGetFlight() throws SQLException, ClassNotFoundException {
//					FlightDao flightDao = new FlightDao();
//			        FlightUpdateDao flightUpdateDao = new FlightUpdateDao();
//			        int rand = Math.abs(new Random().nextInt());
//			        String tableName = "tbl_test_flight" + rand;
//			        System.out.println("Creating table: " + tableName);
//			        flightUpdateDao.createTable(tableName);
//					Flight flight1 = new Flight(2913637, new java.sql.Timestamp(2022, 5, 25, 11, 30, 0, 0), 20, 200.00, new java.sql.Timestamp(2022, 5, 25, 11, 30, 0, 0), "ABC");
//					int attempt = flightUpdateDao.insertFlight(tableName, flight1);
//					System.out.println("employee1 : " + attempt);
//					List<Flight> employeeList = new ArrayList<>();
//					employeeList.add(flight1);
					
					
		//
//			        List<Employee> employeeReturned = flightDao.getFlights(tableName);
//			        assertEquals(employeeList, employeeReturned);
//			        employeeUpdateDao.dropTable(tableName);
//			        System.out.println("Deleted Table: " + tableName);
			    }
	
}